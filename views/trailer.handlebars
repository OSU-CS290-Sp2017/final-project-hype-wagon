<div class="trailer-info">
	<!-- Each is a paragraph which will have its information filled in dynamically -->
	<p class="trailer-title"> Title: <a href="{{url}}">{{title}}</a> </p>
	<p class="trailer-embed"> URL: {{url}} </p>
	<p class="trailer-tags"> Tags: {{tags}} </p>
</div>
